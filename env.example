## Copy to `.env` and fill in values (DO NOT commit `.env`).
## `.env` is already ignored via `.gitignore`.

# Server
PORT=3000

# Pump.fun auth (optional; used by the livechat/trades fetchers)
# PUMPFUN_JWT=
# PUMPFUN_COOKIE=
# PUMPFUN_USERNAME=

# Drive sync output
# DRIVE_SYNC_DIR=MeVoltOBS/DriveSync
# DRIVE_DB_PATH=MeVoltOBS/drive_db.json

# Solana RPC (for /creator-fees and payouts)
# SOLANA_RPC_URL=https://api.mainnet-beta.solana.com
# SOLANA_RPC_URLS= (comma-separated fallbacks)
# SOLANA_RPC_TIMEOUT_MS=8000
# CREATOR_FEES_CACHE_TTL_MS=120000

# Secure payouts (/payout)
# - The browser sends `x-payout-token` (set it via URL param `?payoutToken=...`)
# - The server requires this token to authorize payouts
PAYOUT_TOKEN=change-me

# Treasury keypair secret (server-side signing)
# Supported formats:
# - JSON array from Solana keypair file: "[12,34,...]" (64 bytes typical)
# - Base58 string (some exports): 64-byte secret key OR 32-byte seed
TREASURY_SECRET_KEY=change-me

